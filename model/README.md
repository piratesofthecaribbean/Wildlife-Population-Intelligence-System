# Model

Trained YOLOv8 weights (`best.pt`) and training run artifacts will be stored here.
This folder is git-ignored for `.pt` files (large binaries) — see backend/.gitignore.
Populated in the AI Module.
